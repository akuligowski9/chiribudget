# Supabase Configuration
# Get these from your Supabase project: Settings > API

# Public URL - safe to expose in browser
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co

# Anon key - safe to expose in browser (RLS protects data)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Service role key - NEVER expose in browser, server-side only
# Only needed for admin operations (optional for basic usage)
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Demo Mode Configuration
# Set to 'true' for demo-only deployment (legacy, can be removed)
# When enabled, app auto-enters demo mode with no login UI
# Leave empty or 'false' for main app deployment
NEXT_PUBLIC_DEMO_ONLY=false

# OAuth Allowlist for Demo Site
# Comma-separated list of emails allowed to OAuth on demo site
# Only these users can access real Supabase data from chiribudgetdemo.vercel.app
# Non-allowlisted users are redirected to portfolio site
# Leave empty to block all OAuth on demo site (demo mode still works)
NEXT_PUBLIC_DEMO_ALLOWED_EMAILS=your@email.com,partner@email.com
